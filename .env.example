# This is a static front-end concept site — no environment variables
# are required to run it. This file exists as a placeholder so the
# pattern is established if a backend (order API, CMS, etc.) is added
# later. Copy to .env and fill in real values when that happens.
#
# VITE_API_BASE_URL=
